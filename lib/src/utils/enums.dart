enum HopVideoPlayerVideoState {
  loading,
  playing,
  paused,
  error,
  finished
}

enum HopVideoPlayerType {
  network,
  networkQualityUrls,
  file,
  asset,
  vimeo,
  youtube,
  vimeoPrivateVideos,
}
